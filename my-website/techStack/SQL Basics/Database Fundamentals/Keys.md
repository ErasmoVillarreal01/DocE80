## Primary Key
A primary key (PK) is a **unique** and **identifiable** value for a row in a specific table.
Most often the primary key is implemented with an **ID**.
## Foreign Key
Primary keys of tables can be used in other tables to reference that table. Foreign keys **do not be to be unique**.