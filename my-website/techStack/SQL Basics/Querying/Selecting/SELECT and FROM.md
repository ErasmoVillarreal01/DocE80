- SELECT: Specifies the column
- FROM: Specifies the table

Select a single data column from a table
```SQL
SELECT Email
FROM Customer;
```
Select multiple data columns from a table
```SQL
SELECT Email, LastName, FirstName
FROM Customer;
```
Select all data columns from a table
```SQL
SELECT *
FROM Customer;
```
