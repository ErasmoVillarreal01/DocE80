If statements have an initial statement, optional else if statements and an else statement. These statements need a **boolean expression**. 

If statement in C# structure:
```CS
if(some Boolean expression) {
  //Some action
} else if {
  //Some action
} else {
  //Some other action
}
```
