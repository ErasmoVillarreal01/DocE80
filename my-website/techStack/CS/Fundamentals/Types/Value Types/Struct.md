A struct is a value type that represents a custom data structure. 
Can contain methods and other members.

Example:
```CS
struct WorkTask
{
  public string description;
  public int hours;
	
  public void PerformWorkTask()
  {
    //code
  }
}
```